<?xml version="1.0"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://maven.apache.org/POM/4.0.0" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.nuxeo</groupId>
    <artifactId>nuxeo-parent</artifactId>
    <version>2025.21</version>
  </parent>

  <groupId>org.onlyoffice</groupId>
  <artifactId>onlyoffice-nuxeo-parent</artifactId>
  <version>3.0.0</version>
  <name>ONLYOFFICE Nuxeo Parent</name>
  <description>Bring full office editing to your Nuxeo content platform - edit, co-author, and manage office files without leaving Nuxeo.</description>
  <packaging>pom</packaging>
  <url>https://github.com/ONLYOFFICE/onlyoffice-nuxeo</url>

  <licenses>
    <license>
      <name>Apache License, Version 2.0</name>
      <url>https://www.apache.org/licenses/LICENSE-2.0</url>
      <distribution>repo</distribution>
    </license>
  </licenses>

  <scm>
    <url>https://github.com/ONLYOFFICE/onlyoffice-nuxeo</url>
    <connection>scm:git:https://github.com/ONLYOFFICE/onlyoffice-nuxeo.git</connection>
    <developerConnection>scm:git:git@github.com:ONLYOFFICE/onlyoffice-nuxeo.git</developerConnection>
  </scm>

  <modules>
    <module>onlyoffice-nuxeo-core</module>
    <module>onlyoffice-nuxeo-rest</module>
    <module>onlyoffice-nuxeo-web-ui</module>
    <module>onlyoffice-nuxeo-package</module>
  </modules>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.onlyoffice</groupId>
        <artifactId>onlyoffice-nuxeo-core</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>org.onlyoffice</groupId>
        <artifactId>onlyoffice-nuxeo-rest</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>org.onlyoffice</groupId>
        <artifactId>onlyoffice-nuxeo-web-ui</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>com.auth0</groupId>
        <artifactId>java-jwt</artifactId>
        <version>4.0.0</version>
      </dependency>
      <dependency>
        <groupId>com.onlyoffice</groupId>
        <artifactId>docs-integration-sdk</artifactId>
        <version>1.8.0</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
    <nuxeo.target.version>2025</nuxeo.target.version>
  </properties>

  <build>
    <plugins>
      <!-- Compiler, surefire and jar plugin versions are managed by nuxeo-parent. -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-jar-plugin</artifactId>
        <configuration>
          <archive>
            <manifestFile>src/main/resources/META-INF/MANIFEST.MF</manifestFile>
            <manifestEntries>
              <Bundle-Version>${project.version}-t${buildNumber}</Bundle-Version>
              <Bundle-ManifestVersion>2</Bundle-ManifestVersion>
            </manifestEntries>
          </archive>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-checkstyle-plugin</artifactId>
        <version>3.3.0</version>
        <configuration>
          <!-- Paths are anchored to the reactor root so the same files are used from every module. -->
          <configLocation>${maven.multiModuleProjectDirectory}/checkstyle.xml</configLocation>
          <suppressionsLocation>${maven.multiModuleProjectDirectory}/checkstyle-suppressions.xml</suppressionsLocation>
          <suppressionsFileExpression>org.checkstyle.sun.suppressionfilter.config</suppressionsFileExpression>
          <headerLocation>${maven.multiModuleProjectDirectory}/onlyoffice.header</headerLocation>
          <failsOnError>true</failsOnError>
          <failOnViolation>true</failOnViolation>
          <logViolationsToConsole>true</logViolationsToConsole>
          <violationSeverity>warning</violationSeverity>
          <consoleOutput>true</consoleOutput>
          <includeTestSourceDirectory>true</includeTestSourceDirectory>
        </configuration>
        <executions>
          <execution>
            <goals>
              <goal>check</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

  <repositories>
    <repository>
      <id>maven-public</id>
      <url>https://packages.nuxeo.com/repository/maven-public/</url>
      <releases>
        <enabled>true</enabled>
      </releases>
      <snapshots>
        <enabled>false</enabled>
      </snapshots>
    </repository>
    <!-- Nuxeo LTS artifacts (nuxeo-parent 20xx.*) are only published here; requires packages.nuxeo.com credentials. -->
    <repository>
      <id>maven-private</id>
      <url>https://packages.nuxeo.com/repository/maven-private/</url>
      <releases>
        <enabled>true</enabled>
      </releases>
      <snapshots>
        <enabled>false</enabled>
      </snapshots>
    </repository>
  </repositories>
</project>